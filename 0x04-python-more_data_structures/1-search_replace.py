#!/usr/bin/python3

def search_replace(my_list, search, replace):
	new_list = my_list.copy()
	if len(new_list) > 0:
		i = 0
		m = 0
		for i in my_list:
			if my_list[m]  == search
			   my_list[j] = replace

			j += 1
	return my_list
